# Write a program that prints the number of characters in a user's name. You might need to Google for a function that calculates the length of a string.

name = input("What is your name? ")

num_of_char = len(name)

print(num_of_char)